The in-clip playback position bar — sunken track bed, pine traveled fill, amber car-token thumb, stamped mono times. Lives inside `NowCard` while a clip plays.

```jsx
<Scrubber positionMs={138000} durationMs={405000} onSeek={seekTo} />
```
