A circular header nav-bar button — a subtle ranger-placard disc with a crisp ink glyph. ~40pt, icon-only, so an accessibility label is required.

```jsx
<HeaderIconButton name="settings" accessibilityLabel="Settings" onPress={openSettings} />
<HeaderIconButton name="more" accessibilityLabel="More actions" onPress={openMenu} />
```
