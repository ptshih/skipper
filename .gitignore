# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
.next/
web-build/

# Expo / React Native (apps/mobile). CNG: the native ios/ + android/ dirs are
# GENERATED by `npx expo prebuild` (from app.json + config plugins), so we do NOT
# commit them — native config (e.g. the carplay-audio entitlement) belongs in
# app.json / a config plugin, not hand-edited in the generated project.
.expo/
.expo-shared/
apps/mobile/ios/
apps/mobile/android/
*.orig.*
*.keystore

# TypeScript
*.tsbuildinfo

# Env files (dotenvx). Encrypted .env.development / .env.production ARE committed;
# the plaintext .env, the private keys, and *.local overrides are NOT.
.env
.env.keys
.env.*.local
!.env.example

# Keys — service-account JSON etc. NEVER commit (auth secrets)
keys/
key/
*-sa-key.json

# Logs
*.log

# Scratch / throwaway verification scripts (agent workspace)
.scratch/
# Scratch audio (studio TTS/clip output dumped during verification)
.scratch-audio/
# Design-review agent output (local report artifacts, not committed)
.design-review/
# Playwright MCP output (screenshots / traces dumped during verification)
.playwright-mcp/
# TestFlight feedback dumps (tester PII — screenshots + comments; `bun run tf:feedback`)
testflight-feedback/

# Claude Code: local agent runtime state (locks, worktrees) stays ignored, but
# the SHARED project settings (hooks — the docs lint, the STOP-list guard) and
# the project skills are committed, so every agent on this tree gets the same
# rituals and the same guardrails.
.claude/*
!.claude/settings.json
!.claude/skills/

# Codex
.codex/*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# NOTE: bun.lock (text lockfile) IS committed — do not ignore it.
