{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "//": "*.test.ts run under `bun test` + scripts/ run under `bun` (both provide their own types: bun:test, Bun globals); the app has no @types/bun, so tsc skips them.",
  "exclude": ["node_modules", "scripts", "**/*.test.ts", "**/*.test.tsx"]
}
